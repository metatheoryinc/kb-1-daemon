{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "doc-session",
  "sourceRoot": "packages/doc-session/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/doc-session build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/doc-session test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/doc-session typecheck"
      }
    }
  }
}
