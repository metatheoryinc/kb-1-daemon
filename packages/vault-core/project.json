{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "vault-core",
  "sourceRoot": "packages/vault-core/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/vault-core build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/vault-core test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-1/vault-core typecheck"
      }
    }
  }
}
