{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "vault-service",
  "sourceRoot": "packages/vault-service/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-2/vault-service build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-2/vault-service test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @kb-2/vault-service typecheck"
      }
    }
  }
}
